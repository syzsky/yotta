# Context

- 用户需要“截当前默认目标，帮我创建工作流”的完整旅程，也需要窗口/屏幕截图；MCP 和 AI 提案复用同一底层查询与截图服务。
- 目标配置即用户授权，不增加重复确认或运行热路径通用权限层。AI 提案仍只生成候选，接受后修改工作流。
- 工作流持久 Source 与编辑器当前选中/未保存状态必须区分，不以旧数据伪装当前画布。
- 图片必须通过模型支持的图像通道传递，不能把 base64 当普通 JSON 文字让模型猜。返回实际截图尺寸及坐标空间。
- 现有 docs/ 为开发文档；新增 user-docs/ 面向月离文档导入，采用全局 yueli-docs-publish 的 Markdown/docs.json 包格式。
- 文档脚本复制到仓库，CI 不依赖个人 .codex 路径；集成现有唯一 release.yml 发布流程，手动执行保留 artifact，标签发布附带 docs.zip。未授权 push、上传文档站或创建 Release。
