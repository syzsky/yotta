# 事件运行时整改审查

范围：从检查点 `482c257723ba8a956f813e35da4740a7d52f613e` 到当前工作区，包含未提交及新增文件。两个独立审查分别核对 AGENTS/错误契约与 Workspace 已确认方案，不把既有门禁通过当作审查结论。

## Standards

1. **P1，已修复：后台生产者失败未及时结束常驻任务。** Scope 保存错误后，调度循环及时读取并唤醒失败收尾；panic 也完成计数回收。永久周期任务回归从失败转为通过。
2. **P1，已修复：清理旋转日志尾段后丢失原始错误归属。** 清理前保存有类型的失败，不依赖结束后的64条尾部。66个等待分支中1个失败、65个取消后，错误码、节点、attempt与分类均保留。
3. **P2，已修复：采集服务准备失败无持久运行证据。** 先建立 Run，再准备服务；失败/取消写回记录，保留 plugins.start_failed 等稳定错误码。
4. **P2，已修复：服务准备可能读取不同代的实时设置。** 准备回调消费同次 Run 的 immutable target snapshot；封存来源地址和程序参数，参数切片复制。两个相反的新旧配置组合回归通过。

Standards 复核：四项均解决，未发现新的实质残留问题。没有加入逐操作授权或探测。

## Spec

1. **P1，已修复：持键者与等待输入的兄弟任务导致暂停死锁。** 先冻结新的输入获取，把等待者置为可暂停状态，等待在途动作停稳后释放持键；恢复持键后才解冻获取。覆盖调试暂停与编译后的嵌套监测链路，防止暂停期间漏执行排队点击。监测争用race重复5次通过。
2. **P2，已修复：重复或乱序位置样本被状态写入版本伪装为新样本。** 独立比较来源 sequence/sampleAt/receivedAt，同一epoch严格前进；状态槽重复写入不能凑足两次到达确认。保留过期、坐标基准和epoch检查。

## 验证

- 两个异步失败审查回归：普通与race通过。
- 暂停争用：完整compiler/inputcoord普通通过，定向race通过，监测争用race5次通过。
- 服务准备：引用选择、重复复用、配置覆盖、禁用与失败历史回归通过。
- Acquire基准383.4ns/op，96B/2allocs，无新增分配；相对此前127ns增加约0.00026ms。
- 最终仓库门禁与新版无面板冷启动实测结论以 Work index 为准；不把上述回归代替实际入口验收。

## 冷启动验收补充

采集服务启动后，首批坐标可能超过1秒才可用。原移动节点过早返回坐标不可用，已让首次采样使用节点超时，后续保留短时恢复及松键保护。1500ms延迟来源回归先红后绿；新版真实WebView关闭面板并停止采集器后，两次HTTP运行成功，重新冷启动的移动图正确输出到达。验收使用克隆资料，未修改用户目标坐标。

验证环境备注：并行完整Go检查时，两项真实时钟的模拟移动测试触发500ms坐标过期保护而停步，定向空闲回归及真实冷启动均通过；最终门禁改为GOFLAGS=-p=1串行。Windows原生输入首次缺少WM_CHAR，保持窗口稳定后完整独立重跑通过。实际用户资料修订23已在最终App编译打开，读取前后sourceJson完全一致。

最终结论：完整串行门禁通过，154文件/645前端测试通过；冷启动位置race在前端测试结束后独立通过30.416秒。两轴全部发现已修复，未保留未解决的实质审查项。实时样本保护仍可能在严重系统负载下主动停止移动，此处未放宽生产阈值。
